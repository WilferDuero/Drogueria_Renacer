# Entorno del backend (ejemplo seguro)
PORT=3001
NODE_ENV=development

# Seguridad JWT (OBLIGATORIO en produccion)
JWT_SECRET=CAMBIA_ESTA_CLAVE_LARGA_Y_PRIVADA
JWT_TTL=8h

# Usuario owner inicial (solo se crea si la tabla users esta vacia)
ADMIN_USER=admin
ADMIN_PASS=CAMBIA_ESTA_CLAVE
ADMIN_ROLE=owner

# DB
# Si DATABASE_URL existe usa Postgres; si no, usa SQLite local
# DATABASE_URL=postgres://USER:PASS@HOST:5432/DBNAME?sslmode=require
# PG_SSL=true
# SQLITE_DIR=./data

# CORS
# Lista separada por comas. Si se omite, usa allowlist por defecto (Vercel + localhost).
# CORS_ORIGINS=https://drogueria-renacer.vercel.app,http://localhost:3000,http://127.0.0.1:3000
